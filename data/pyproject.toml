[project]
name = "data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.13.1"
dependencies = [
    "prefect==3.6.7",
]

[tool.uv.workspace]
members = ["common", "flows/*", "deploy"]
